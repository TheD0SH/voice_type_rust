{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Capability for the main settings window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
